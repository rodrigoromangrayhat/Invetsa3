<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.grayhatcorp.invetsa.invetsa">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Login"
            android:label="@string/app_name" />
        <activity
            android:name=".SistemaIntegralMonitoreo"
            android:label="SIM (Sistema Integral de Monitoreo)" />
        <activity
            android:name=".HojaDeVerificacion"
            android:label="HOJA DE VERIFICACION" />
        <activity
            android:name=".MaquinaTwinShot"
            android:label="MAQUINA TWIN SHOT" />
        <activity
            android:name=".Principal"
            android:label="INVETSA">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MaquinaSpravac"
            android:label="MAQUINA SPRAVAC"></activity>
        <activity android:name=".Touch">
        <activity
            android:name=".Maquina_zootec"
            android:label="MAQUINA ZOOTEC"></activity>



        </activity>
    </application>

</manifest>